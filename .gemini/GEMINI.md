## Gemini Added Memories

- The user prefers to skip confirmation for actions that do not involve file changes (e.g., reading, searching,
  listing). Confirmation is still required for file modifications or system-altering shell commands.
- The user wants me to only ask for confirmation for major changes or implementations, not for small, line-by-line
  changes.
- The user wants to always allow all command execution and skip confirmation.
- The user prefers that I do not commit changes automatically. I should only stage changes and inform the user, allowing
  them to commit manually.
- The user prefers that I do not compile or commit changes for subsequent requests.
- The user wants me to run `yarn lint` command on changed files and omit the errors
