FROM node:22-alpine AS builder
WORKDIR /app

COPY apps/frontend-consumer/package.json apps/frontend-consumer/package-lock.json ./
RUN npm ci

COPY apps/frontend-consumer/ .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY docker/nginx/consumer.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
